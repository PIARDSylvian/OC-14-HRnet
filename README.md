# HRnet

Internal management solution.

## how to run project

### without Docker

install project with
`npm i`

build project with
`npm run build`

lunch project with
`npm run preview`

### with Docker

`docker compose up -d`
